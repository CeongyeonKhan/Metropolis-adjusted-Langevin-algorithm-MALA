seed: 20260829
device: cpu
dtype: float64
target:
  family: bimodal
  dim: 2
  separation: 4.0
error:
  kind: fourier
  ensemble_size: 5
  features: 64
  relative_rmse: 0.2
calibration:
  num_points: 8192
adaptation:
  h0: 0.04
  h_max: 0.5
  gamma: 1.0
global_proposal:
  probability: 0.05
  gaussian_scale: 2.0
  student_scale: 4.0
  student_weight: 0.05
  degrees_of_freedom: 5.0
sampler:
  methods: [B1, B2, A1, A2-O, A2-P, A3, A4]
  num_chains: 64
  num_steps: 50000
  initialization: single_mode
  diagnostic_stride: 10
  trajectory_stride: 50
  checkpoints: [0, 10, 20, 50, 100, 200, 500, 1000, 2000, 5000, 10000, 20000, 50000]
metrics:
  fid_reference_repeats: 500
wandb:
  mode: offline
  project: ege-ah-mala
  group: P5-full
output:
  root: outputs
  experiment_name: p5_full

