# 交付前验证记录

验证日期：2026-08-29。

## 静态检查与单元测试

```bash
ruff check src tests scripts
pytest -q
```

结果：代码规范检查通过，23 项测试全部通过。测试覆盖解析梯度、海森矩阵、海森矩阵—向量积、通用 \(\gamma\) 局部核、全局混合密度、校准、白化、诊断、逐链跨模态删失及命令行端到端路径。

## 端到端运行

执行了三类独立验证：

1. `configs/smoke.yaml` 的四方法 CPU 运行，W&B 禁用；
2. A4 的 W&B 离线运行，包括标量、图像、摘要和实验制品记录；
3. `configs/p7_highdim.yaml` 的缩小预算运行，确认固定探针海森矩阵—向量积曲率路径可执行。

三类运行均正常退出。生成的 JSON 与 JSONL 文件均通过禁止 `NaN`/`Infinity` 的严格解析；W&B 离线运行未产生初始化降级文件。

验证产物未打包进项目压缩文件，避免把临时输出、缓存和本地 W&B 运行混入源码交付物。
